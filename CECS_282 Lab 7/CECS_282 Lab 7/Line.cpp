using namespace std;
#//include "Line.h"
/*
 template <typename Point, typename T> Line<Point, T>::Line(T x1, T y1, T x2, T y2) {
	begin =  Point<T>(x1, y1);
	end = Point<T>(x2, y2);
}
  template <typename Point, typename T> Line<Point, T>::Line(Point begin, Point end) {
	this->begin = begin;
	this->end = end;
}
// Public getter and setter for private variables
 template <typename Point, typename T> Point Line<Point, T>::getBegin() {
	return begin;
}
 template <typename Point, typename T> Point Line<Point, T>::getEnd() {
	return end;
}

 template <typename Point, typename T> void Line<Point, T>::setBegin(Point p) {
	begin = p;
}
 template <typename Point, typename T> void Line<Point, T>::setEnd(Point p) {
	end = p;
}
//toString method
 template <typename Point, typename T> string Line<Point, T>::toString() {
	return "Line: [" + begin.toString() + ", " + end.toString() + "] " + "Length: " + to_string(getLength());
}
//DReturns the length of the line
 template <typename Point, typename T> double Line<Point, T>::getLength() {
	double length = sqrt(pow((end.getX() - begin.getX()), 2) + pow((end.getY() - begin.getY()), 2));
	return length;
}
 */