using namespace std;
/*
#include "Point.h"


	// Constructors
    template class Point<T>::Point(T x, T y) {
		this->x = x;
		this->y = y;
	}
	template <class T> Point<T>::Point() {
		// default
		x = 0;
		y = 0;
	}
	template <class T> T Point<T>::getX() {
		return x;
	}
	template <class T> T Point<T>::getY() {
		return y;
	}
	template <class T> void Point<T>::setX(T x) {
		this->x = x;
	}
	template <class T> void Point<T>::setY(T y) {
		this->y = y;
	}
	// toString()
	template <class T> string Point<T>::toString() {
		string sx = to_string(x);
		string sy = to_string(y);
		return "[ " + sx + ", " + sy + "]";
	}
	*/